#! /bin/csh -f

# @(#) $Header: /home/deyke/tmp/cvs/tcp/tools/tcpmake,v 1.10 1994-01-10 08:48:21 deyke Exp $

tcpremove
chmod 755 ~/tcp/lib/configure
~/tcp/lib/configure > ~/tcp/lib/configure.h
foreach dir ( \
	~/tcp \
	~/tcp/bbs \
	~/tcp/convers \
	~/tcp/doc \
	~/tcp/examples \
	~/tcp/lib \
	~/tcp/src \
	~/tcp/tools \
	~/tcp/util )
	cd $dir
	if (-f Makefile) then
		cp -p Makefile Makefile.old
		make depend
		if ( { cmp -s Makefile Makefile.old } ) then
			rm Makefile
			mv Makefile.old Makefile
		else
			rm Makefile.old
		endif
	endif
	trimfiles
end
cd ~/tcp/src
makeall |& tee xxx
cd ~/tcp
make
mk make
