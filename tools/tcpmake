#! /bin/csh -f

# @(#) $Header: /home/deyke/tmp/cvs/tcp/tools/tcpmake,v 1.8 1993-11-06 17:00:16 deyke Exp $

tcpremove
chmod 755 ~/tcp/configure
~/tcp/configure > ~/tcp/lib/configure.h
foreach dir ( \
	~/tcp \
	~/tcp/bbs \
	~/tcp/convers \
	~/tcp/doc \
	~/tcp/lib \
	~/tcp/src \
	~/tcp/tools \
	~/tcp/util )
	cd $dir
	if (-f Makefile) then
		cp -p Makefile Makefile.old
		make depend
		if ( { cmp -s Makefile Makefile.old } ) then
			mv Makefile.old Makefile
		else
			rm Makefile.old
		endif
	endif
	trimfiles
end
cd ~/tcp/src
makeall |& tee xxx
cd ~/tcp
make
mk make
